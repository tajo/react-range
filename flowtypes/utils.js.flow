// @flow

declare export function getTrackBackground(params: {
  min: number,
  max: number,
  values: number[],
  colors: string[],
  direction?: string;
}): string;

declare export function useThumbOverlap(params: {
  rangeRef: HTMLElement,
  values: number[],
  index: number,
  separator?: string,
}): mixed[];